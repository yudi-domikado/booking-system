<h2><span>Thank you</span>, your booking is complete</h2>  
<p>Your comfirmation number is <span>223686</span></p>          
<article id="content" class="cols-d">
  <div>
    <h3>Information</h3> 
    <% if user_signed_in? %>
      <h4 class="scheme-g">User Infomation</h4>
      <ul class="list-c">
        <li><span>Full Name</span> <%= current_user.name %></li>
        <li><span>Email Address</span> <span class="email"><%= current_user.email %></span></li>
    <% else %>
      <h1><span>Hello,Guest!</span></h1>
      <br/>
      <% end %>
    </ul>
    <ul class="list-c">  
      <% @cart.cart_items.each do |cart_item| %>
        <li><span>Room Name</span><%= cart_item.room.name %></span></li>
        <li><span>Picture</span>
        <%= image_tag cart_item.room.picture.url(:medium) %>
        </li>
        <li><span>Room price</span><%= number_to_currency(cart_item.room.price, unit: "Rp.", format: "%u %n") %></span></li>
        <li><span>Check in Date</span><%= cart_item.check_in_date.strftime('%d-%B-%Y') %></span></li>
        <li><span>Start Time</span><%= cart_item.start_time.strftime('%H:%M:%S') %></span></li>
        <li><span>End Time</span><%= cart_item.end_time.strftime('%H:%M:%S') %></span></li>     
      <% end %>
    </ul>  
    <p class="link-c">
    <%= link_to "Reservation", check_out_users_path %>
    <!--   <a href="./">Print receipt</a> -->
    </p>
  </div>
  <aside>
    <h3>Summary</h3>
    <div class="vcard a">
      <h4 class="fn org">Domikado Meeting Room</h4>
      <p class="adr">Domikado - <span class="locality">Jakarta</span>, <span class="country-name">Indonesia</span></p>
    </div>
    <h4 class="scheme-g">Summary</h4>
    <ul class="list-c">
     
    </ul>
  </aside>
</article>