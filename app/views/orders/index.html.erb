

<div id="content" class="clearfix">
  <div id="left-area" class="clearfix full-wide"> 

<h1>Order <span>List</span></h1>

<table class="table">
  <thead >
  	<tr>
 	<th>ID</th>
 	<th>Total</th>
  <th>User</th>
  <th>Menu</th>
  <th>Picture</th>
  <th>Start Time</th>
  <th>End Time</th>
    </tr>
  </thead>
  <tbody>

		<% @orders.each do |order| %>
    <!-- dalam masalah loop..yang di loop tr nya -->
    		<tr>
    			<td><%= link_to order.id, order_path(order) %></td>
					<td>
					  <%= number_to_currency(order.ammount, format: "%u %n") %>
					</td>
          <td>
            <%= order.user.name %>
          </td>
            <% order.order_items.each do |order_item| %>  
          <td>
            <%= order_item.room.name  %> 
          </td> 
          <td>
            <%= image_tag order_item.room.picture.url(:thumb)%>
          </td>
          <td>
            <%= order_item.start_time.strftime('%H:%M:%S') %>
          </td>
          <td>
            <%= order_item.end_time.strftime('%H:%M:%S') %>
          </td>
          <% end %>

				</tr>
	</tbody>

<% end %>

</table>
</div>
</div>
